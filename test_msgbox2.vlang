external function MessageBoxA(hwnd: u64, text: ptr<u8>, title: ptr<u8>, flags: u32) -> i32
external function ExitProcess(code: u32) -> void

function main() -> i32 {
    MessageBoxA(0, "Hello", "Test", 0)
    ExitProcess(0)
    return 0
}
